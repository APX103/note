# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Private local-only project drafts (keep out of the repo)
wandlink/

# ZCode 本地会话状态
.zcode/

# 自研唤醒词：虚拟环境与可再生的训练数据（README 有复现命令）
embedded/magic-wand/wakenet/.venv/
embedded/magic-wand/wakenet/data/

# 编辑器/Agent 插件根目录标记
.v2c/
.video_agent/

# luxo_mujoco: 虚拟环境与 PPO 检查点(可再生)
luxo_mujoco/.venv/
luxo_mujoco/models/
luxo_mujoco/__pycache__/

# pixar-lamp: 渲染中间帧(可由 mujoco_render.py 重新生成)
embedded/pixar-lamp/mujoco/results/frames/
**/__pycache__/
